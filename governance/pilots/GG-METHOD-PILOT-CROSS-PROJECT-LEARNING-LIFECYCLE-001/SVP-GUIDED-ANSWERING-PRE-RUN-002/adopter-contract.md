---
package_id: GG-METHOD-PILOT-CROSS-PROJECT-LEARNING-LIFECYCLE-001-SVP-GA-PRE-RUN-002
pilot_id: GG-METHOD-PILOT-CROSS-PROJECT-LEARNING-LIFECYCLE-001
status: NON_NORMATIVE_PILOT_CANDIDATE
adopter: Sugar144/stakeholder-validation-portal
vertical: SVP-STAGE2A-GUIDED-ANSWERING-001
historical_predecessor: GG-METHOD-PILOT-CROSS-PROJECT-LEARNING-LIFECYCLE-001-SVP-GA-PRE-RUN-001
---

# SVP adopter contract — cross-project learning lifecycle pilot successor

## 1. Ownership boundary

General Governance owns only the pilot semantics in this package. SVP owns the adopter projection, exact implementation-packet binding, and all empirical learning evidence it produces.

This remains a non-normative pilot. It does not amend General Governance `0.1.0-rc.8` and does not change Guided Answering requirements, architecture, story scope, or implementation authority.

No cross-project signal transfers implementation, execution, retry, replacement, merge, release, deployment, acceptance, risk, or normative authority to another project.

## 2. Current adopter reference state

Remote evidence observed at successor preparation:

- repository: `Sugar144/stakeholder-validation-portal`;
- remote `main`: `0d3bc042e3c1236efaff7ff2fbcf78eb2c7bb772`;
- vertical: `SVP-STAGE2A-GUIDED-ANSWERING-001`;
- vertical status: `IMPLEMENTATION_AUTHORIZED_NOT_STARTED`;
- canonical next gate: `BEGIN_GUIDED_ANSWERING_IMPLEMENTATION_ST_01`;
- V1 status: `ACCEPTED_INTEGRATED_CLOSED`.

PRE-RUN-001's historical C2 preparation SHA `280aab9c202394cca4107c3ac3df0885be6a9164` remains historical evidence only and is not remotely resolvable. PRE-RUN-002 deliberately does not bind that SHA as current.

Before the first relevant ST-01 execution boundary is crossed, the separately authorized SVP adopter package MUST bind and prove the exact then-current implementation-packet/WPDC identity, including repository branch/HEAD, packet identity, review state, coordination/admission state, and all other applicable preconditions. Any inability to prove exact currentness is a STOP.

## 3. Material event-time capture

Capture only material reusable causal learning, including material required-gate failure or indeterminacy, material independent-review finding, Owner correction revealing a method/authority weakness, near miss, validator/harness false positive or negative, tooling/orchestration failure affecting execution truth, scope/currentness ambiguity STOP, replacement/recovery/strategy-change evidence, material method/tooling waste, or a material control that prevented an unsafe or unauthorized effect.

Do not create durable learning merely for ordinary TDD red/green cycles, trivial typos, harmless warnings, formatting corrections, or low-value one-off failures. Existing truthful governed reports may satisfy event-time capture; one file per event is not required.

## 4. Packet terminal disposition

Before governance-terminal closure of every Guided Answering packet admitted into this pilot, declare exactly one:

- `NO_MATERIAL_LEARNING`;
- `LOCAL_LEARNING_RECORDED`;
- `CROSS_PROJECT_SIGNAL_RECORDED`.

The latter two require durable references. A cross-project signal may identify candidate target projects but creates no authority there.

Each later packet must have its own current bounded adopter identity; no packet inherits implementation authority from PRE-RUN-002 or from another packet.

## 5. Vertical learning closeout

At learning closeout of `SVP-STAGE2A-GUIDED-ANSWERING-001`, declare exactly one:

- `NO_CROSS_PROJECT_SIGNALS`; or
- `HARVEST_RECORDED`.

A full cross-project harvest is required iff at least one constituent governed packet declared `CROSS_PROJECT_SIGNAL_RECORDED`. Harvesting aggregates existing evidence; it must not fabricate, rewrite, or retrospectively present missing evidence as contemporaneous.

## 6. Maximum candidate SVP pilot write surface

This GG package defines only a maximum candidate surface. It performs no SVP write and grants no SVP authority. A separately authorized adopter package may narrow but not widen it:

1. `docs/governance/learning/README.md` — create if absent; discoverable index only.
2. `docs/governance/learning/GG-METHOD-PILOT-CROSS-PROJECT-LEARNING-LIFECYCLE-001.yaml` — adopter-owned prospective pilot binding.
3. the exact current Guided Answering implementation packet/work-package report bound by the SVP adopter package — learning references, measurements, and terminal disposition only.
4. `reports/work-packages/SVP-STAGE2A-GUIDED-ANSWERING-LEARNING-CLOSEOUT-001.md` — one vertical learning closeout, created only at closeout.
5. `reports/work-packages/SVP-STAGE2A-GUIDED-ANSWERING-CROSS-PROJECT-LEARNING-HARVEST-001.md` — conditional; create only when at least one packet emitted a cross-project signal.

The adopter package MUST replace item 3 with one exact current path before any execution. A wildcard, stale C2 name, or inferred path is insufficient.

## 7. Measurements

Preserve only actual observations. Do not invent token, time, cost, count, or friction data when unavailable. Measure only what can support the Issue #12 decision: packet disposition, material learning count/type, cross-project signals/targets, retrospective-only discovery, Owner reminder dependence, observable avoided noise/duplicates, lifecycle friction, materiality ambiguity, and usefulness/rigidity of deterministic checks.

## 8. Pre-run conditions and STOP rules

Before ST-01 crosses its separately authorized implementation boundary, SVP must prove:

- an exact current adopter branch and HEAD;
- an exact current implementation-packet/WPDC identity and report path;
- applicable WPDC validity and dependency closure;
- applicable independent review freshness/PASS;
- coordination/admission gate state required by current governance;
- the adopter pilot projection is already materialized prospectively;
- AET-P4 and P5B remain separately satisfied where still applicable;
- any Claude Agent SDK empirical run has an exact component set and separate prospective Owner authority.

STOP on identity/currentness drift, stale or indeterminate review, missing packet binding, write-surface drift, product/architecture scope drift, inferred AET-P4/P5B/RUN authority, or any need to reconstruct learning evidence.

## 9. Integration boundary

PRE-RUN-002 must itself be independently reviewed and separately integrated into GG before it can be used as the main-reachable GG pilot reference for an SVP adopter projection.

No SVP mutation, ST-01 implementation, or Claude Agent SDK RUN-001 is authorized by this artifact.