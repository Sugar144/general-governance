---
prompt_id: GG-MP-0011
version: 1.0.0
status: EXECUTED
category: OWNER_AUTHORIZATION
custody_date: 2026-09-22
---

# Material Prompt — rc.11 Owner authorization

## Exact Project Owner prompt text

```text
adelante!
```

## Applied authorization scope

This authorization is applied prospectively and narrowly to the General Governance successor release packaging and integration block presented immediately before the prompt: prepare `0.1.0-rc.11` as the coherent successor to the accepted `0.1.0-rc.10` candidate, bind the already-qualified Factory runner migration into the scoped release payload, compute and validate the exact `SCOPED_TRACKED_FILES_V1` identity, satisfy Gate A and Gate B plus required repository checks, integrate the exact qualified candidate through PR #40, record the resulting Owner disposition, and allow ASF to re-anchor General Governance to `main` once that integration is complete.

The authorization does not permit weakening release-payload classification, excluding `.github/workflows/conformance-ci.yml`, bypassing branch protection or required checks, or reusing the immutable rc.10 content identity for changed release-included bytes.

## Forbidden effects

This prompt is not interpreted as authorization for:

- a Git tag or GitHub Release;
- release publication or deployment;
- production-readiness claims;
- provider/model execution;
- mutation of unrelated framework semantics or capabilities;
- bypass of consumer-contract, project-state, review, branch-protection, or portfolio-conformance gates;
- downstream adopter rebinding beyond the ASF portfolio-ref reconciliation explicitly described above.

The exact rc.11 candidate identity and final integration commit are recorded separately after deterministic qualification; this material prompt preserves the Owner instruction as received and does not itself assert technical success.
